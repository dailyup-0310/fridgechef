# 产品需求文档（PRD v1.2）

## 产品名称：FridgeChef（冰箱厨神）

---

# 1. 引言与概述

## 1.1 项目背景

用户在日常饮食场景中普遍面临以下问题：

* 冰箱中已有食材无法有效利用（不知道可以做什么）
* 决策成本高（需反复搜索与筛选菜谱）
* 健康饮食缺乏执行路径

现有菜谱类产品以“浏览”为核心，缺乏“基于已有食材直接生成方案”的能力。

---

## 1.2 产品目标

本产品旨在提供一个**基于用户已有食材的智能菜谱生成工具（Web端）**，实现：

* 快速生成可执行菜谱
* 降低用户决策成本
* 提供基础健康饮食辅助能力

---

## 1.3 产品定位

> 一个面向轻决策场景的 AI 菜谱生成工具（工具型产品）

核心特征：

* 快速（输入到结果 ≤ 数秒级）
* 低认知负担（无需学习成本）
* 强结果导向（直接输出可执行方案）

---

# 2. 产品目标（SMART）

| 目标项    | 指标定义          |
| ------ | ------------- |
| 生成完成率  | ≥ 80%用户完成生成流程 |
| 首次生成时长 | ≤ 30秒         |
| 菜谱点击率  | ≥ 60%         |
| 周使用频率  | ≥ 2次/用户       |

---

# 3. 目标用户

## 3.1 用户类型

### 上班族（22–40岁）

* 时间有限
* 不愿主动决策

### 学生群体

* 食材有限且零散
* 需要高利用率方案

### 减脂/健康用户

* 有热量控制目标
* 缺乏饮食搭配能力

---

## 3.2 核心需求

* 基于已有食材生成菜谱
* 降低决策成本
* 提供健康饮食参考

---

# 4. 用户故事与使用场景

## 用户故事

* 用户输入已有食材，希望快速获得可执行菜谱
* 用户希望减少操作步骤，快速得到结果
* 减脂用户希望获得符合热量目标的菜谱

---

## 使用场景流程

1. 用户进入页面
2. 输入食材
3. 选择偏好（可选）
4. 点击生成
5. 查看推荐菜谱

---

# 5. 用户流程（User Flow）

```text
进入页面
  ↓
默认日常模式
  ↓
输入食材
  ↓
选择偏好（可选）
  ↓
点击生成
  ↓
Loading状态（动画+文案）
  ↓
展示3个菜谱
  ↓
用户行为：
  - 查看详情
  - 再来一次
  - 重新开始
```

---

# 6. 核心功能列表

| 功能   | 描述       | 优先级 |
| ---- | -------- | --- |
| 模式切换 | 日常/减脂    | P0  |
| 食材输入 | 多输入框+标签  | P0  |
| 偏好选择 | 参数选择     | P0  |
| 菜谱生成 | AI生成3个结果 | P0  |
| 结果展示 | 卡片展示     | P0  |
| 再生成  | 刷新结果     | P0  |

---

# 7. 功能需求（详细）

---

## 7.1 模式切换模块

### 描述

页面顶部切换控件，用于切换生成逻辑。

### 模式

* 日常模式（默认）
* 减脂模式

### 系统行为

* 切换模式时：
  👉 **清空全部输入数据（食材 + 偏好）**

---

## 7.2 食材输入模块

### 输入方式

* 默认3个输入框
* 支持动态新增输入框

### 功能要求

* 支持中文与英文输入
* 提供常见食材标签
* 支持删除输入项

---

### 输入处理（系统必须实现）

* 同义词归一（如：番茄=西红柿）
* 英文转中文（如：chicken→鸡肉）

---

### 提示机制

* 当输入食材较少时提示：
  “食材越多，可生成菜品越丰富”

---

## 7.3 偏好选择模块

### 日常模式

| 参数   | 类型 | 必填 |
| ---- | -- | -- |
| 餐别   | 枚举 | 否  |
| 做饭时长 | 枚举 | 否  |
| 菜系   | 枚举 | 否  |
| 口味   | 枚举 | 否  |

---

### 减脂模式

| 参数   | 类型         | 必填 |
| ---- | ---------- | -- |
| 餐别   | 枚举         | 否  |
| 做饭时长 | 枚举         | 否  |
| 热量目标 | 数值输入（kcal） | 否  |

---

## 7.4 菜谱生成模块

### 输入

* 食材列表
* 用户偏好
* 当前模式

---

### 输出（每个菜）

| 字段   | 说明          |
| ---- | ----------- |
| 菜名   | 文本          |
| 食材列表 | 用户食材 + 基础调料 |
| 做法步骤 | 分步骤说明       |
| 图片   | 图片URL       |
| 热量   | 减脂模式显示      |

---

### 核心约束

1. 仅使用用户输入食材
2. 默认允许基础调料（盐、油、葱姜蒜等）

---

### 异常处理

当无法生成菜谱时：

* 提示：“当前食材无法组合成菜”
* 提供建议：“建议增加1–2种食材”

---

### 热量逻辑（减脂模式）

* 为估算值
* 接近用户输入目标
* 允许浮动范围（建议±20%）
* 每个菜必须展示热量

---

## 7.5 结果展示模块

### 布局

* 三个卡片并列

### 卡片内容

* 图片
* 菜名
* 热量（减脂模式）

---

### 交互

* 点击卡片 → 展开详情（当前页面）

---

## 7.6 再生成模块

* 使用相同输入重新生成
* MVP阶段不限制次数

---

## 7.7 Loading模块

* 动画（烹饪相关）
* 文案提示（如：主厨正在准备中）

---

# 8. 设计说明

## 8.1 设计原则

* 参考Figma原型图

---

## 8.2 页面结构

### 主页面

* 模式切换栏
* 食材输入区
* 偏好选择区
* 固定生成按钮

---

### 结果区域

* 三卡片展示
* 点击展开详情

---

# 9. 技术方案

## 9.1 技术目标

本项目为 Web 端 MVP，优先实现核心功能闭环，不实现用户登录、数据持久化及支付能力。

---

## 9.2 前端技术栈

* **Next.js**：构建页面及服务端能力
* **TypeScript**：增强代码可维护性
* **Tailwind CSS**：快速实现界面样式
* **shadcn/ui**：组件库（按钮、卡片、输入框等）
* **Framer Motion**：实现动效（切换、卡片展开、Loading）

---

## 9.3 服务端方案

使用 **Next.js API Routes** 实现轻量后端能力：

负责：

* 接收用户输入
* 校验参数
* 构建AI请求
* 调用外部API
* 返回结构化结果

---

## 9.4 第三方能力接入

### AI生成

* 使用 **OpenAI API**
* 负责：

  * 菜谱生成
  * 食材归一化
  * 热量估算
  * 异常兜底

---

### 图片服务

* 使用 **Pexels API / Unsplash API**
* 根据菜名检索真实图片

---

## 9.5 数据存储策略

* MVP阶段不使用数据库
* 用户数据仅在当前会话中生效

---

## 9.6 部署方案

* 使用 **Vercel** 部署
* 支持快速上线与迭代

---

## 9.7 数据监测

建议接入基础分析工具，用于监测：

* 生成点击率
* 菜谱点击率
* 再生成使用率
* 模式使用占比

可选工具：

* Vercel Analytics
* Google Analytics

---

## 9.8 后续扩展预留

当需要以下能力时引入数据库：

* 登录系统
* 收藏与历史记录
* 用户偏好
* 订阅与支付


---

# 10. 成功指标

* 菜谱生成成功率
* 菜谱点击率
* 用户停留时长
* 再生成使用率

---

# 11. 版本历史

## v1.0.0（内测版）— 2026-04-20

### 新增功能

**邀请码访问控制**
- 首次访问须输入邀请码，验证通过后 Cookie 保存 30 天
- 邀请码通过环境变量 `INVITE_CODES` 配置，格式：`CODE:次数上限`，支持多个码
- 使用次数以**每次生成菜谱**计算（非每次打开页面）
- 计数持久化至 Supabase（Singapore 节点），跨设备、跨重启共享计数

**菜谱生成优化**
- 食材上限从 8 个提升至 15 个
- 菜谱食材严格限制为用户提供的食材 + 基础调料，不得出现用户未提供的主食材
- 口味偏好精确化：重口仅限麻辣/酱爆/红烧/水煮类，普通炒菜归入"适中"
- 食材不足时返回推荐添加食材，而非报错
- 所有菜谱已推荐完时显示"已经是所有菜谱啦"提示

**图片服务**
- Pexels 图片匹配度过低（`total_results < 5`）时自动降级为默认占位图
- 默认占位图改为静态 PNG（`/images/recipe-placeholder.png`）

**交互体验**
- 加载中替换为趣味烹饪动画（锅盖弹跳 + 蒸汽 + 循环文案）
- 菜谱详情页图片尺寸缩小

---

## v1.1.0 — 2026-04-20

### 更新内容

**邀请码管理升级**
- 邀请码从环境变量迁移至 Supabase `invite_codes` 表管理
- 在 Supabase Table Editor 中插入新行即生效，删除行即失效，无需重新部署

---

# 12. 未来规划

* 用户系统
* 收藏功能
* 个性化推荐
* 订阅制
